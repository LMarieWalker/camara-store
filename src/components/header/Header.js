import React from 'react';
import Brand from './Brand';
import Nav from './Nav';

const Header = (props) => {
  return (
    <div>
      <Brand />
      <Nav />
    </div>
  );
};

export default Header;
